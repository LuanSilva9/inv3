import UserTimeResponse from "../../Constants/UserTimeResponse";

export default function HeroSection() {
    return (
        <section className="hero-section">
            <h2>Olá, Usuario(a)<UserTimeResponse/></h2>
        </section>
    )
}